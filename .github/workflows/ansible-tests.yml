# This workflow has been split into multiple smaller workflows for better maintainability.
# Please use the main workflow dispatcher: ansible-tests-main.yml
# Individual workflows:
# - lint-and-syntax.yml: YAML/Ansible linting and syntax validation
# - unit-tests.yml: Unit test execution
# - integration-tests.yml: Integration and check mode tests
# - security-scan.yml: Security scanning and validation
# - mock-device-tests.yml: Mock device framework testing
# - molecule-tests.yml: Molecule container testing
# - comprehensive-tests.yml: Critical gap and comprehensive test suites

name: Ansible Tests (Deprecated - Use ansible-tests-main.yml)

on:
  workflow_dispatch:
    inputs:
      migration_notice:
        description: 'This workflow is deprecated. Use ansible-tests-main.yml instead.'
        required: false
        default: 'acknowledged'

jobs:
  migration-notice:
    name: Migration Notice
    runs-on: ubuntu-latest
    steps:
      - name: Migration Notice
        run: |
          echo "⚠️  MIGRATION NOTICE ⚠️"
          echo "This workflow (ansible-tests.yml) has been deprecated and split into smaller workflows."
          echo ""
          echo "🔄 Please use the new main workflow: ansible-tests-main.yml"
          echo ""
          echo "📋 Individual workflows available:"
          echo "  - lint-and-syntax.yml: YAML/Ansible linting and syntax validation"
          echo "  - unit-tests.yml: Unit test execution"
          echo "  - integration-tests.yml: Integration and check mode tests"
          echo "  - security-scan.yml: Security scanning and validation"
          echo "  - mock-device-tests.yml: Mock device framework testing"
          echo "  - molecule-tests.yml: Molecule container testing"
          echo "  - comprehensive-tests.yml: Critical gap and comprehensive test suites"
          echo ""
          echo "✅ Benefits of the new structure:"
          echo "  - Improved maintainability and readability"
          echo "  - Faster parallel execution"
          echo "  - Better organization by test category"
          echo "  - Easier debugging and troubleshooting"
          echo ""
          echo "🚀 The new workflows are production-ready and fully tested!"