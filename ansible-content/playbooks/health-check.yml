---
# Health Check Playbook
# Standalone playbook version for direct execution

- name: Health Check Playbook
  hosts: "{{ target_hosts }}"
  gather_facts: false
  tasks:
    - name: Include health check tasks
      ansible.builtin.include_role:
        name: common
        tasks_from: health-check
