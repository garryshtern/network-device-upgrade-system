---
# Single platform mock inventory for focused testing
# Useful for testing specific platform logic

all:
  children:
    cisco_nxos:
      hosts:
        test-nxos-01:
          ansible_host: localhost
          ansible_connection: local
          ansible_network_os: cisco.nxos.nxos
          firmware_version: "9.3.10"
          target_version: "10.1.2"
          device_model: "N9K-C93180YC-EX"
          issu_capable: true
      vars:
        ansible_user: test_user
        ansible_password: test_password
