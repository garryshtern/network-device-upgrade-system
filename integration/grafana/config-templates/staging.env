# Staging Environment Configuration
# Grafana Dashboard Provisioning - Network Device Upgrade Management System

# Grafana Configuration
GRAFANA_URL="https://grafana-staging.networkops.company.com"
GRAFANA_ADMIN_USER="grafana-admin"
# Note: Set GRAFANA_ADMIN_PASS via secure environment management
# export GRAFANA_ADMIN_PASS="$(cat /etc/secrets/staging/grafana-admin-password)"

# InfluxDB Configuration
INFLUXDB_URL="https://influxdb-staging.networkops.company.com:8086"
INFLUXDB_ORG="network-operations-staging"
INFLUXDB_BUCKET="network-upgrade-metrics-staging"
# Note: Set INFLUXDB_TOKEN via secure environment management
# export INFLUXDB_TOKEN="$(cat /etc/secrets/staging/influxdb-token)"

# Optional Slack Integration (Staging Channel)
# SLACK_WEBHOOK_URL="https://hooks.slack.com/services/your/staging/webhook"

# Staging-specific Settings
GRAFANA_FOLDER_NAME="Network Device Upgrades (Staging)"
ENABLE_ALERTING="true"
ENABLE_VALIDATION="true"

# Security Settings (relaxed for staging)
REQUIRE_TLS="true"
VERIFY_SSL_CERTS="false"  # May use self-signed certs in staging

# Performance Settings
MAX_CONCURRENT_DEPLOYMENTS="2"
DEPLOYMENT_TIMEOUT="180"  # 3 minutes
DASHBOARD_REFRESH_OVERRIDE="45s"  # Slightly faster than production